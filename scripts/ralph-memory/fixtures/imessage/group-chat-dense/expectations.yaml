# Dense window from group chat - Fight Talk
description: "Dense window from group chat - verify multi-person extraction at scale"

entities:
  must_have:
    - name_contains: "Tyler"
      entity_type: Person

  optional:
    - name_contains: "Casey"
      entity_type: Person
    - entity_type: Event
    - entity_type: Location

relationships:
  optional:
    - relation_type: KNOWS
    - relation_type: ATTENDED
    - relation_type: SCHEDULED_FOR

episode_entity_mentions:
  must_have:
    - entity_name_contains: "Tyler"
