version: 2

builds:
  - main: ./cmd/comms
    binary: comms
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
      - -X main.commit={{.Commit}}
      - -X main.buildDate={{.Date}}

brews:
  - repository:
      owner: Napageneral
      name: homebrew-tap
    homepage: https://github.com/Napageneral/comms
    description: Unified communications cartographer

archives:
  - format: tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
